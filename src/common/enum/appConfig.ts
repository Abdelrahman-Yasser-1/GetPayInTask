export enum THEME_VARIANT {
  DARK = 'dark',
  LIGHT = 'light',
}

export enum APP_LANGUAGES {
  EN = 'en',
  AR = 'ar',
}
// just incase they change there key we update it from here
export enum API_LANGUAGES {
  EN = 'en',
  AR = 'ar',
}
